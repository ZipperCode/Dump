<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ProgressBar
        android:id="@+id/pb_common_loading"
        android:layout_width="@dimen/loading_progress_bar_size"
        android:layout_height="@dimen/loading_progress_bar_size"
        android:layout_gravity="center"
        android:indeterminateDrawable="@drawable/layer_list_progress_loading"
        android:minWidth="@dimen/loading_progress_bar_size"
        android:indeterminateBehavior="repeat"
        android:minHeight="@dimen/loading_progress_bar_size" />

</FrameLayout>